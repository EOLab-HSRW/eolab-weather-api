version: '3.7'

services:
  weather-api:
    image: sirsundays/weather-api
    build:
      context: .
    ports:
      - "8079:8079"
    environment:
      - PGHOST=
      - PGPORT=
      - PGUSER=
      - PGPASSWORD=
      - PGDATABASE=
